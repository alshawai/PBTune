# Database
psycopg2-binary==2.9.11
SQLAlchemy==2.0.46

# Data processing
pandas==3.0.1
numpy==2.4.2

# System monitoring
psutil==7.2.2

# Environment variables
python-dotenv==1.2.1

# Machine Learning (if needed)
scikit-learn==1.8.0

# Configuration
PyYAML==6.0.3

# Population Based Training
# (No specific library - we implement from scratch)

# Parallel processing
# (Using built-in multiprocessing and concurrent.futures)

# Workload benchmarking
# Note: SYSBENCH installed separately via system package manager

# Evaluation (evaluate_tuning module)
docker>=7.0.0          # Docker SDK for Python — container lifecycle management

# Knob importance analysis
fanova>=2.0.0
pyrfr>=0.8.0
shap>=0.44.0
jenkspy>=0.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
scipy>=1.11.0
# Note for contributors: fanova depends on pyrfr, which may require SWIG and C++ build tools on some systems.
# If pip install fails, check pyrfr installation docs.

# Bayesian Optimization
ConfigSpace>=1.1.0
smac>=2.4.0
